{
    "env_name": "${uvms.env.name}",
    "rest_api_base" : "${uvms.rest.base.url}:${uvms.rest.base.port}",
    "ws": {
    	"host": "${uvms.rest.base.url}",
    	"port": "${uvms.rest.base.port}"
    }
}
