describe('mapAlarmsService', function() {

  beforeEach(module('unionvmsWeb'));

  it('should ...', inject(function(mapAlarmsService) {

	//expect(mapAlarmsService.doSomething()).toEqual('something');

  }));

});