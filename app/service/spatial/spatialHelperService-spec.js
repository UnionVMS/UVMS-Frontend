describe('spatialHelperService', function() {

  beforeEach(module('unionvmsWeb'));

  it('should ...', inject(function(spatialHelperService) {

	//expect(spatialHelperService.doSomething()).toEqual('something');

  }));

});