describe('defaultMapConfigs', function() {

  beforeEach(module('unionvmsWeb'));

  it('should ...', inject(function(defaultMapConfigs) {

	//expect(defaultMapConfigs.doSomething()).toEqual('something');

  }));

});