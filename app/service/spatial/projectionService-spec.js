describe('projectionService', function() {

  beforeEach(module('unionvmsWeb'));

  it('should ...', inject(function(projectionService) {

	//expect(projectionService.doSomething()).toEqual('something');

  }));

});