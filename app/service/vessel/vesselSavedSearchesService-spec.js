describe('vesselSavedSearchesService', function() {

  beforeEach(module('unionvmsWeb'));

  it('should ...', inject(function(vesselSavedSearchesService) {

	//expect(savedsearches.doSomething()).toEqual('something');

  }));

});
