describe('mdrRestService', function() {

  beforeEach(module('unionvmsWeb'));

  it('should ...', inject(function(mdrRestService) {

    //expect(mdrRestService.doSomething()).toEqual('something');

  }));

});
