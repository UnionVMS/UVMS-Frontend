describe('pollListPage', function() {

  beforeEach(module('unionvmsWeb'));

  it('should ...', inject(function(PollListPage) {

	//expect(pollListPage.doSomething()).toEqual('something');

  }));

});