describe('subscriptionsService', function() {

  beforeEach(module('unionvmsWeb'));

  it('should ...', inject(function(subscriptionsService) {

    //expect(subscriptionsService.doSomething()).toEqual('something');

  }));

});
