describe('subscriptions', function() {

  beforeEach(module('unionvmsWeb'));

  it('should ...', inject(function(Subscription) {

    //expect(subscriptions.doSomething()).toEqual('something');

  }));

});