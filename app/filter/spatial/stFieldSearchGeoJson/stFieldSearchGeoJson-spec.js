describe('stFieldSearchGeoJson', function() {

	beforeEach(module('unionvmsWeb'));

	it('should ...', inject(function($filter) {

        var filter = $filter('stFieldSearchGeoJson');

		expect(filter('input')).toEqual('output');

	}));

});